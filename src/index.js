import React from "react";
import ReactDOM from "react-dom/client";
import Folymarket from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <Folymarket />
  </React.StrictMode>
);
